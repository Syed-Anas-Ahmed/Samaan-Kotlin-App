<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Samaan" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!--<item name="colorPrimary">@color/my_light_primary</item>-->

        <item name="colorPrimary">@color/g_gray500</item>
        <item name="colorPrimaryVariant">@color/g_white</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status bar = responsible for the background color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Status bar text colors handle from here -->
        <item name="android:windowLightStatusBar" tools:ignore="NewApi">true</item>

    </style>

    <style name="Theme.Samaan" parent="Base.Theme.Samaan" />
</resources>